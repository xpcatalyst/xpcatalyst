import { describe, it, expect, vi } from "vitest";
import { mountSuspended } from "@nuxt/test-utils/runtime";
import { type Project } from "../../types/project.types";

describe("ProjectPage [id]", () => {
    // TODO: Test with real project data and route parameters
    it.todo("Renders the project details with the correct project data")
    it.todo("Displays a message when the project does not exist")
})