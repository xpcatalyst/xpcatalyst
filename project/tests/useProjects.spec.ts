import { describe, it, expect } from 'vitest'
// import { mount } from '@vue/test-utils'
// import { mountSuspended } from '@nuxt/test-utils/runtime'
// import { type Project, mockProjects, ProjectList, PROJECTS_NB, NO_PROJECTS_MESSAGE } from '@/project'

describe('useProjects', () => {
    it.todo('Should update projects when fetchProjects is successful');
    it.todo('Should set error when fetchProjects encounters a network error');
    it.todo('Should increment stars when projectId matches');
    it.todo('Should increase displayed projects count when loadMoreProjects is called');
    it.todo('Should initialize displayed projects with the first PROJECTS_NB projects') 
});
