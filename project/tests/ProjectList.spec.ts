import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import {type Project, mockProjects, ProjectList } from '@/project'
describe('ProjectList', () => { 
    it.todo('Should render the correct number of project items', () => { 

        const projects = mockProjects as Project[]

    })
    it.todo('Should display a message when no projects are available')
})