import { describe, it, expect, vi, beforeEach } from 'vitest'
// import { mountSuspended } from '@nuxt/test-utils/runtime'
// import { ProjectListItem, type ProjectSummary } from '..'

describe('ProjectListItem', () => {
  it.todo('Should render the name and the description of the project');
  it.todo("Should call the composable's incrementStars function when the button is clicked");
  it.todo("Should not call incrementStars when already starred");
  it.todo('Should disable the button when isStarred is true');
});