import { describe, it, expect } from 'vitest'
// import { mountSuspended } from '@nuxt/test-utils/runtime' // => Icon component which could be async
// import { ProjectList, generateMockProjects, PROJECTS_NB, NO_PROJECTS_MESSAGE } from '..'

describe('ProjectList', () => {
  it.todo('Should render correct number of project items');
  it.todo('Should display N skeleton projects on loading state');
  it.todo('Should display warning component with a message when no projects are specified');
})